'use strict';

const _ = require('lodash'),
      utils = require('../../../utils'),
      driver = utils.getDriver(),
      C = driver.constants;

module.exports = function (object, intents, scope) {
    const gameTime = scope.gameTime,
          bulk = scope.bulk,
          roomController = scope.roomController;

    if (roomController) {}
};
//# sourceMappingURL=../../../sourcemaps/processor/intents/terminal/pretick.js.map
